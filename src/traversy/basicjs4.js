//DOM

console.log(window)
//window.alert("fdf")

const tasks= document.querySelector('.par');
/*
tasks.forEach(
    function(key){
        console.log(key.textContent);
    }
)
*/

//tasks.lastElementChild.remove()
//tasks.lastElementChild.textContent='test';

//e.preventDefault(); this methods disallows default behavior of event like click of submit button not to submit first